# V1 — fMP4 + dedup tests

**Repo:** sworld-backend
**Type:** test
**Estimate:** S–M
**Blocked by:** P1, P2
**Blocks:** —
**Parallel-safe with:** —

## Context

Lock in both outcomes: one core, fMP4 output — so neither silently regresses.

## Scope

- **Format**: each path's output is fMP4 — playlist has `#EXT-X-MAP:URI="init.mp4"`
  + `.m4s` (not `.ts`); `init.mp4` uploaded as `video/mp4`;
  `ffprobe -map 0:a -f null -` on output is clean.
- **Dedup**: a guard test / lint that the CLI imports the core and contains no
  re-implemented `parseM3U8`/`uploadSegments`/finalize/`slugify` (catch drift).
- One small e2e on a known HE-AAC fixture end-to-end → asserts fMP4.

## Files to touch (ownership)

- test files alongside F1/F2/P1/P2 — **V1 owns the format + dedup assertions.**

## Acceptance criteria

- [ ] A test fails if any path emits `.ts` again.
- [ ] A test fails if the CLI re-grows inline processing.
- [ ] CI green.

## Out of scope

- Perceptual/browser audio testing (covered manually per P1/P2).
- Catalog migration (G1).
