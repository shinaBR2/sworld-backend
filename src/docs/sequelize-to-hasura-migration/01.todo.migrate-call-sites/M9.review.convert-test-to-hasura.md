# M9 — Migrate `compute/videos/routes/convert` test to Hasura `completeTask`

**Repo:** sworld-backend
**Type:** migration (test only)
**Estimate:** XS
**Blocked by:** H1
**Blocks:** D1
**Parallel-safe with:** M7, M8

## Context

The convert handler itself does not import `src/database`, but its **test** still
imports and mocks `completeTask` from `src/database/queries/tasks`. Re-point the
test's import + `vi.mock` to the H1 Hasura module so D1 can delete
`src/database/`. Not in the original consumer map — found during the D1 readiness
check.

## Scope

- In `convert/index.test.ts`, change the `completeTask` import and its `vi.mock`
  target from `src/database/queries/tasks` to
  `src/services/hasura/mutations/tasks`. The `completeTask` assertions are
  unchanged.

## Files to touch (ownership)

- `src/apps/compute/videos/routes/convert/index.test.ts`

## Acceptance criteria

- [ ] No `src/database` import remains in the test.
- [ ] Test assertions unchanged and passing.
- [ ] biome lint clean.

## Test plan

- `npx vitest run src/apps/compute/videos/routes/convert`

## Out of scope

- The convert handler (it does not touch `src/database`).
- Deleting `src/database/queries/tasks.ts` (D1).
