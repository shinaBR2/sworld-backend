# M5 — Migrate `fix-videos-duration` gateway route to Hasura

**Repo:** sworld-backend
**Type:** migration (call site)
**Estimate:** XS
**Blocked by:** H1
**Blocks:** D1
**Parallel-safe with:** M1, M2, M3, M4, M6

## Context

The `fix-videos-duration` gateway route lists videos missing a duration via the
Sequelize `getVideoMissingDuration` (`src/database/queries/videos`), then enqueues
fix tasks. Swap the read to H1's Hasura `getVideoMissingDuration` — a 1:1 import
swap; the route's enqueue logic is untouched.

## Scope

- Replace the `getVideoMissingDuration` import from `src/database/queries/videos`
  with H1's Hasura query from `src/services/hasura/queries/videos`.
- Confirm the returned shape (the fields the route reads, e.g. `id`/`source`)
  matches what H1 selects; adjust the selection set in H1 if a field is missing
  (coordinate, but it should already be covered).
- Update the route's test to mock the Hasura helper.

## Files to touch (ownership)

- `src/apps/gateway/videos/routes/fix-videos-duration/index.ts`
- `src/apps/gateway/videos/routes/fix-videos-duration/index.test.ts` (if present)

## Acceptance criteria

- [ ] No `src/database` import remains in the route.
- [ ] The missing-duration list is fetched via Hasura.
- [ ] Enqueue behaviour unchanged.
- [ ] Tests pass; `pnpm lint` + `tsc` clean.

## Test plan

- `pnpm vitest run src/apps/gateway/videos/routes/fix-videos-duration`

## Out of scope

- The downstream `fix-duration` io handler (M2).
- Deleting `src/database/queries/videos.ts` (D1).
