# M7 — Migrate `gateway/videos/routes/crawl` to Hasura task enums

**Repo:** sworld-backend
**Type:** migration (call site)
**Estimate:** XS
**Blocked by:** H1
**Blocks:** D1
**Parallel-safe with:** M8, M9

## Context

The gateway crawl route imports `TaskEntityType` / `TaskType` from the Sequelize
model (`src/database/models/task`). Re-point to the H1 Sequelize-free constants
module so D1 can delete `src/database/`. Not in the original consumer map — found
during the D1 readiness check.

## Scope

- Swap the enum import in the handler and its test to
  `src/services/hasura/mutations/tasks/constants`.

## Files to touch (ownership)

- `src/apps/gateway/videos/routes/crawl/index.ts`
- `src/apps/gateway/videos/routes/crawl/index.test.ts`

## Acceptance criteria

- [ ] No `src/database` import remains in the route or its test.
- [ ] Enqueue behaviour unchanged.
- [ ] Tests pass; biome lint clean.

## Test plan

- `npx vitest run src/apps/gateway/videos/routes/crawl`

## Out of scope

- Deleting `src/database/models/task.ts` (D1).
