# M6 — Migrate `fix-videos-thumbnail` gateway route to Hasura

**Repo:** sworld-backend
**Type:** migration (call site)
**Estimate:** XS
**Blocked by:** H1
**Blocks:** D1
**Parallel-safe with:** M1, M2, M3, M4, M5

## Context

The `fix-videos-thumbnail` gateway route lists `ready` videos missing a thumbnail
via the Sequelize `getVideoMissingThumbnail` (`src/database/queries/videos`), then
enqueues fix tasks. Swap the read to H1's Hasura `getVideoMissingThumbnail` — a
1:1 import swap; the route's enqueue logic is untouched.

## Scope

- Replace the `getVideoMissingThumbnail` import from `src/database/queries/videos`
  with H1's Hasura query from `src/services/hasura/queries/videos`.
- Confirm the returned shape (the fields the route reads) matches what H1
  selects; adjust H1's selection set if a field is missing (should already be
  covered).
- Update the route's test to mock the Hasura helper.

## Files to touch (ownership)

- `src/apps/gateway/videos/routes/fix-videos-thumbnail/index.ts`
- `src/apps/gateway/videos/routes/fix-videos-thumbnail/index.test.ts` (if present)

## Acceptance criteria

- [ ] No `src/database` import remains in the route.
- [ ] The missing-thumbnail list is fetched via Hasura.
- [ ] Enqueue behaviour unchanged.
- [ ] Tests pass; `pnpm lint` + `tsc` clean.

## Test plan

- `pnpm vitest run src/apps/gateway/videos/routes/fix-videos-thumbnail`

## Out of scope

- The downstream `fix-thumbnail` io handler (M3).
- Deleting `src/database/queries/videos.ts` (D1).
