# M4 — Migrate `crawler/crawl` to Hasura `completeTask`

**Repo:** sworld-backend
**Type:** migration (call site)
**Estimate:** XS
**Blocked by:** H1
**Blocks:** D1
**Parallel-safe with:** M1, M2, M3, M5, M6

## Context

The crawler route completes its task with the Sequelize `completeTask`
(`src/database/queries/tasks`). It's a standalone task completion (no video
write), so it just needs H1's standalone `completeTask` Hasura helper — a 1:1
import swap.

## Scope

- Replace the `completeTask` import from `src/database/queries/tasks` with H1's
  `completeTask` from `src/services/hasura/mutations/tasks`.
- Confirm the call signature (`completeTask({ taskId })`) is unchanged.
- Update `crawler/crawl/index.test.ts` (if it mocks the DB helper) to mock the
  Hasura helper instead.

## Files to touch (ownership)

- `src/apps/io/crawler/routes/crawl/index.ts`
- `src/apps/io/crawler/routes/crawl/index.test.ts` (if present)

## Acceptance criteria

- [ ] No `src/database` import remains in the crawler route.
- [ ] Task completion goes through Hasura.
- [ ] Tests pass; `pnpm lint` + `tsc` clean.

## Test plan

- `pnpm vitest run src/apps/io/crawler/routes/crawl`

## Out of scope

- Any other crawler behaviour.
- Deleting `src/database/queries/tasks.ts` (D1).
